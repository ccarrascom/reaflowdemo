import {css} from '@emotion/css';

const DEMO_CONTROL_HEIGHT = '64px';

export const Header = css`
  width: 100%;
  display: flex;
  height: ${DEMO_CONTROL_HEIGHT};
  justify-content: center;
  background-color: var(--color-white);
`;
